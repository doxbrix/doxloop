# Doxloop brand assets

- `doxloop-logo-light.png` is the wordmark for light backgrounds.
- `doxloop-logo-dark.png` is the wordmark for dark backgrounds.
- `doxloop-favicon.png` is the square application and browser icon.

Keep the transparent backgrounds intact. When both wordmarks can be supplied,
select between them using the viewer's preferred color scheme.
